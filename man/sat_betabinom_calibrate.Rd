% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sat_betabinom_calibrate.R
\name{sat_betabinom_calibrate}
\alias{sat_betabinom_calibrate}
\title{Calibrate posterior cutoff gamma and estimate power (Single-Arm, Beta–Binomial)}
\usage{
sat_betabinom_calibrate(
  alpha = 0.1,
  p_t = 0.8,
  n_t = 40,
  M = 0.65,
  prior = c("flat", "jeffreys", "beta"),
  a_base = 1,
  b_base = 1,
  tol = 0.002,
  seed = 123,
  show_progress = FALSE
)
}
\arguments{
\item{alpha}{Target Type-I error rate (numeric in \verb{(0,1)}).}

\item{p_t}{True response probability in treatment group (numeric in \verb{[0,1]}).}

\item{n_t}{Sample size in treatment group (integer).}

\item{M}{Decision threshold for theta (numeric in \verb{[0,1]}).}

\item{a_base, b_base}{Numeric. Hyperparameters of the Beta prior (used when \code{prior = "beta"}).}

\item{tol}{Absolute tolerance for calibration convergence.}

\item{seed}{RNG seed.}

\item{show_progress}{Logical. Whether to display progress bars.}
}
\value{
A list with components:
\itemize{
\item \code{gamma}: Calibrated posterior probability threshold.
\item \code{type1}: Estimated Type-I error at \code{gamma}.
\item \code{power}: Estimated Bayesian power at \code{gamma}.
\item \code{settings}: Echo of input parameters.
}
}
\description{
Calibrates the posterior threshold \code{gamma} to control Type-I error at level \code{alpha},
and estimates Bayesian power using a conjugate Beta–Binomial model.
}
\examples{
\donttest{
res <- sat_beta_calibrate(
  alpha = 0.10, p_t = 0.80, n_t = 40, M = 0.65,
  prior = "flat"
)
res$gamma
res$power
}
}
\author{
Thomas Debray \email{tdebray@fromdatatowisdom.com}
}
